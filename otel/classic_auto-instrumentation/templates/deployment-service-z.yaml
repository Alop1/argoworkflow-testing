{{- if .Values.serviceZ.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.serviceZ.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.serviceZ.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.serviceZ.name }}
    spec:
      containers:
        - name: {{ .Values.serviceZ.name }}
          image: {{ .Values.serviceZ.image }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: SERVICE_NAME
              value: {{ .Values.serviceZ.name | quote }}
            - name: REDIS_HOST
              value: {{ .Values.redis.serviceName | quote }}
            - name: REDIS_PORT
              value: {{ .Values.redis.port | quote }}
            - name: OTEL_EXPORTER_OTLP_TRACES_ENDPOINT
              value: {{ .Values.otel.tracesEndpoint | quote }}
{{- end }}
